
# Database Configuration (PostgreSQL for Render, MySQL for local)
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/inventory_dbs?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:somya282021}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=${DB_DIALECT:org.hibernate.dialect.MySQLDialect}

# Server Configuration
server.port=${PORT:8081}

# Production Settings (Render will override these)
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true